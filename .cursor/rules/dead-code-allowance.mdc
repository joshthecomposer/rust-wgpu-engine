---
description: Do not suppress dead code diagnostics with allow attributes
alwaysApply: true
---

# Dead Code Diagnostics

- Never add `#[allow(dead_code)]` or similar dead-code suppression attributes.
- If dead code warnings appear, leave them alone unless the user explicitly asks to remove or use the code.
- Prefer ignoring existing dead code diagnostics over adding suppression noise.

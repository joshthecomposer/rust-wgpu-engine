use crate::ui::game_new::styles::Rect;

pub fn compute_layout(_available: Rect) {
    // layout computation is currently handled directly by widgets
    // this module is reserved for future shared layout algorithms
}

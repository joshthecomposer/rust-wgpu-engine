[alias]
# Build dist/ + start python http.server (default port 8080). Use: cargo web -- 9000
web = "run --bin serve-web"

[target.wasm32-unknown-unknown]
rustflags = ["--cfg", "getrandom_backend=\"wasm_js\""]
